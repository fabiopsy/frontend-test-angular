import { FiltraPromocoesPipe } from './filtra-promocoes.pipe';

describe('FiltraPromocoesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraPromocoesPipe();
    expect(pipe).toBeTruthy();
  });
});
